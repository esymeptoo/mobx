{
  "name": "esy-mobx",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "ts-node --project ./server/tsconfig.json server/index.ts",
    "build:server": "rm -rf dist && cross-env NODE_ENV=production tsc -p ./server -outDir dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "mobx": "^4.2.1",
    "ts-loader": "^4.3.0",
    "ts-node": "^6.0.3",
    "typescript": "^2.8.3"
  }
}
